var Serializable = require('./_serializable');

var Model = Serializable.extend({
  // Instance methods & properties
});

Model.reopenClass({
  // Class methods & properties
});

module.exports = Model;
